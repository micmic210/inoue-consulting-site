---
layout: base.njk
title: ブログ
lang: ja
---

<div id="blog-page">
  <section class="container page-intro">
    <h1 class="text-center">ブログ</h1>
    <p class="text-center text-muted">展示会・視察・通訳現場からのレポート</p>
  </section>

  <section class="container blog-grid">
    {% for post in collections.blog_ja %}
      <article class="blog-card">
        <div class="thumb">
          <img src="{{ post.data.thumb or post.data.cover }}" alt="{{ post.data.title }}">
        </div>
        <div class="card-body">
          <h2 class="post-title">{{ post.data.title }}</h2>
          <p class="excerpt">{{ post.data.description }}</p>
          <a class="btn-read" href="{{ post.url }}">続きを読む</a>
        </div>
      </article>
    {% endfor %}
  </section>
</div>